# Docx Renderer Tool

`render_docx.py` provides a minimal reusable OOXML writer for `.docx` reports.

The renderer accepts a title and generic sections. It does not define Redis RDB report content, risk rules, or analysis structure; those remain under `skills/redis-rdb-analysis/`.
